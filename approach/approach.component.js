'use strict';
angular.module('approach', [
  'ngRoute'
]);
// Register `phoneList` component, along with its associated controller and template
angular.
  module('approach').
  component('approach', {
    templateUrl: 'approach/approach.html'}
  );
